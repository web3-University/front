# 默认使用公共npm仓库
# registry=https://registry.npmjs.org/

# @web3-university开头的包使用私有仓库, 其他包使用公共npm仓库
@web3-university:registry=http://localhost:4873

# 启用依赖提升，让子包共享根目录的依赖
hoist=true
hoist-pattern[]=*

# 启用完全提升，解决依赖解析问题
shamefully-hoist=true

# 启用 workspace 协议
link-workspace-packages=true

# 发布配置
access=public
always-auth=false

